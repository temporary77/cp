#include <bits/stdc++.h>
using namespace std;

pair<double,double> arr[100001];

int main() {
	int n, m;
	scanf("%d%d",&n,&m);
	for (int i = 0; i < n; ++i) {
		scanf("%lf%lf",&arr[i].first,&arr[i].second);
	}
	int a, b;
	for (int i = 0; i < m; ++i) {
		scanf("%d%d",&a,&b);
		
	}
	return 0;
}