#include <bits/stdc++.h>
using namespace std;

int arr[100001];
int dp[100001][5];

int main() {
	int t;
	scanf("%d",&t);
	for (int ti = 0; ti < t; ++ti) {
		int n, k, z;
		scanf("%d%d%d",&n,&k,&z);
		for (int i = 0; i < n; ++i) {
			dp[0] = 1
		}
	}
	return 0;
}