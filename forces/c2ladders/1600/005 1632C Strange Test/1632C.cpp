#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	scanf("%d",&t);
	for (int ti = 0; ti < t; ++ti) {
		int a, b;
		scanf("%d%d",&a,&b);
		if (a >= b) {
			printf("%d\n",a-b);
			continue;
		}
		
	}
	return 0;
}