#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    char arr[100000];
    scanf("%d",&n);
    for (int i = 0; i < n; ++i) {
        scanf("%[^\n]",arr);
    }
    printf("No Solutions");
    return 0;
}
